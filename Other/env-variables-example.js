// process  = global provided by node
// env      = property on process, which is an object of all the env variables
// NODE_ENV = a specific env variable

// what's passing through is an object, a collection of variables
console.log(process.env.NODE_ENV);